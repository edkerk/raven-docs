# Names that look like functions but are not functions of either toolbox.
#
# check_names.py resolves every camelCase / snake_case identifier in the
# hand-written pages against the RAVEN and raven-toolbox sources. Anything
# listed here is exempt. Keep the list short and grouped: each entry is a name
# that nothing can verify for you, so an entry added carelessly re-opens exactly
# the hole this check exists to close.

# -- prose terms ------------------------------------------------------------
camelCase
snake_case
Path                          # pathlib, used by several examples
raven_toolbox                 # the Python package name, not a function
libSBML                       # third-party library

# -- cobrapy ----------------------------------------------------------------
# cobra is not installed in the docs build environment, so its names cannot be
# resolved automatically. Only the ones actually used in the pages are listed.
optimize
slim_optimize
get_by_id
summary
Configuration
read_sbml_model
add_boundary
write_sbml_model
flux_variability_analysis
loopless_solution
fraction_of_optimum          # flux_variability_analysis argument
runParallel                  # getAllowedBounds argument
single_gene_deletion
single_reaction_deletion
double_gene_deletion
knock_out_model_genes
moma
next
iter
find_blocked_reactions
check_mass_balance
Metabolite                    # cobra.Metabolite, constructed in the building page
repr
pfba
minimal_medium
load_workbook               # openpyxl, used by the Excel example

# -- RAVEN model struct fields and function arguments -----------------------
allowExcretion                # findLeakMetabolite argument
allowNewGenes                 # addRxns argument
arrayData                     # the expression struct ftINIT and scoreComplexModel take
hpaData                       # ftINIT argument
metabolomicsData              # ftINIT argument
INITSteps                     # ftINIT argument
gene_scores                   # ftinit argument
spontRxnNames                 # prepINITModel argument
extComp                       # prepINITModel argument
convertGenes                  # prepINITModel argument
essential_cache_path          # prep_init_model argument
eqnType                       # addRxns argument
minFlux                       # solveLP argument
removePrefix                  # importModel argument
sortIds                       # exportToExcelFormat / writeYAMLmodel argument
sort_ids                      # the raven-toolbox spelling of the same argument
expected_uptake_count         # key in a condition YAML file
grRules
rxnNames
metNames
metFormulas
rxnGeneMat
rxnsToAdd
stoichCoeffs
allowNetProduction
allow_net_production          # connect_blocked_reactions argument
useModelConstraints

# -- variables and identifiers used in worked examples ----------------------
iAL1006                       # model name in legacy tutorial 1
modelA                        # a stand-in model name in prose
modelB
bmOUT
acOUT
glyOUT
biomassOUT
smallYeast                    # the example model, named in prose
getEssential                  # checkTasks argument
glcIN                         # smallYeast.yml, docs/data/
ethIN
o2IN
newConnected
addedRxns
euk90_kegg105                 # KEGG HMM library artefact

# -- cobrapy attributes named in the guide ----------------------------------
# Attributes, not functions, so they resolve nowhere; the guide names them when
# explaining how the RAVEN struct and cobra.Model correspond.
gene_reaction_rule
lower_bound
upper_bound

# -- helpers that live in a model repository, not in RAVEN ------------------
# hanpo-GEM ships these alongside the reconstruction protocol.
prepHumanModelForftINIT       # Human-GEM, code/tINIT/
importTsvFile                 # Human-GEM, code/io/
newCommit
newRelease
addLipidReactions             # SLIMEr
addSLIMEreactions             # SLIMEr

# -- functions of released RAVEN 2.x, referenced as history -----------------
# These no longer exist on develop3 (the branch this site documents) but are
# named in the pages precisely to say so. Do not add a name here just because
# the checker flagged it -- confirm first that the page discusses it as removed.
importExcelModel              # removed in RAVEN/#631
exportModelToSIF              # removed in RAVEN/#650
drawMap                       # plotting/ dropped in RAVEN/#614
drawPathway                   # plotting/ dropped in RAVEN/#614
setOmicDataToRxns             # plotting/ dropped in RAVEN/#614
getMetaCycModelForOrganism    # external/metacyc dropped in RAVEN/#614
add_loopless
randomObjective              # randomSampling 'method' value, not a function
random_objective             # random_sampling method= value, not a function
